<%= form_with(model: buy_article) do |form| %>
  <% if buy_article.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(buy_article.errors.count, "error") %> prohibited this buy_article from being saved:</h2>

      <ul>
        <% buy_article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :buy_id, style: "display: block" %>
    <%= form.text_field :buy_id %>
  </div>

  <div>
    <%= form.label :article_id, style: "display: block" %>
    <%= form.text_field :article_id %>
  </div>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.text_field :quantity %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
