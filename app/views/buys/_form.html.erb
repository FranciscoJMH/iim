<div class="container-fluid">
  <div class="row">
    <%= form_with(model: buy) do |form| %>
      <% if buy.errors.any? %>
        <div style="color: red">
          <h2>Favor de seleccionar una obra.</h2>
        </div>
      <% end %>

      <table class="table">
        <tb>
          <th>
            <div class="col">
              <%= form.label :Proyecto_de_construcción, style: "display: block",class:"m-2" %>
              <%= form.collection_select(:project_id, Project.all, :id, :name, { prompt: 'Seleccione Obra'}, { class: 'form-control' }) %>
            </div>
          </th>
          <th>
            <div class="col">
              <%= form.label :iva, style: "display: block", class:"m-2" %>
              <%= form.text_field :iva, class: 'form-control' %>
            </div>
            <%= form.hidden_field :user_id, value: current_user.id %>
          </th>
        </tb>
    </table>
            <div>
              <%= form.submit 'Guardar', class: 'btn btn-primary' %>
            </div>
    <% end %>
  </div>
</div>